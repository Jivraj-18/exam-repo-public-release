import { html } from "https://cdn.jsdelivr.net/npm/lit-html@3/lit-html.js";

export default async function ({ user, weight = 1 }) {
  const id = "q-embeddings-use";
  const title = "Purpose of Embeddings";

  const answer = "semantic similarity";

  const question = html`
    <div class="mb-3">
      <p>
        Embeddings generated by language models are primarily used to
        measure what property between pieces of text?
      </p>
      <label for="${id}" class="form-label">Answer:</label>
      <input class="form-control" id="${id}" name="${id}" />
    </div>
  `;

  return { id, title, weight, question, answer };
}
