import { html } from "https://cdn.jsdelivr.net/npm/lit-html@3/lit-html.js";

export default async function ({ user, weight = 1 }) {
  const id = "q-embedding-similarity-math";
  const title = "Embeddings: Similarity Metric";

  const answer = "cosine similarity";

  const question = html`
    <div class="mb-3">
      <p>
        You are comparing two normalized embedding vectors generated by
        <code>text-embedding-3-small</code>.
      </p>
      <p>
        Which mathematical operation correctly measures semantic similarity
        between the two vectors?
      </p>
      <label for="${id}" class="form-label">Metric name:</label>
      <input class="form-control" id="${id}" name="${id}" />
    </div>
  `;

  return { id, title, weight, question, answer };
}
